<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission
        android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>
    <uses-permission
        android:name="android.permission.CAMERA"/>
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="studio3"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App Links for shared piece/series URLs. api.studio-3.co is the deployed
                 backend — the host AppLinkConfig.shareBaseUrl actually generates links
                 against — and serves assetlinks.json for itself at its own /.well-known/
                 (src/modules/share/well_known_routes.py) with the real release-signing
                 SHA-256 fingerprint. studio-3.co is a placeholder for the web app's own
                 domain, once it is pointed at the service — see
                 lib/config/app_link_config.dart. The Render staging host this app used
                 before the AWS move is retired and no longer listed. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="studio-3.co"
                    android:pathPrefix="/piece"/>
                <data
                    android:scheme="https"
                    android:host="api.studio-3.co"
                    android:pathPrefix="/piece"/>
                <data
                    android:scheme="https"
                    android:host="studio-3.co"
                    android:pathPrefix="/series"/>
                <data
                    android:scheme="https"
                    android:host="api.studio-3.co"
                    android:pathPrefix="/series"/>
                <!-- Events are shareable, and a scanned QR lands on /share/... — the path the
                     app's own links use. Matching only /piece would mean a link this app
                     generated opens the app and then does nothing. -->
                <data
                    android:scheme="https"
                    android:host="studio-3.co"
                    android:pathPrefix="/event"/>
                <data
                    android:scheme="https"
                    android:host="api.studio-3.co"
                    android:pathPrefix="/event"/>
                <data
                    android:scheme="https"
                    android:host="studio-3.co"
                    android:pathPrefix="/share"/>
                <data
                    android:scheme="https"
                    android:host="api.studio-3.co"
                    android:pathPrefix="/share"/>
                <!-- The backend's OG-preview routes. AppLinkConfig.pieceUrl generates this
                     shape, so without these every shared link opened the web page instead
                     of the app even on a device that has it installed. -->
                <data
                    android:scheme="https"
                    android:host="studio-3.co"
                    android:pathPrefix="/share/piece"/>
                <data
                    android:scheme="https"
                    android:host="api.studio-3.co"
                    android:pathPrefix="/share/piece"/>
                <data
                    android:scheme="https"
                    android:host="studio-3.co"
                    android:pathPrefix="/share/series"/>
                <data
                    android:scheme="https"
                    android:host="api.studio-3.co"
                    android:pathPrefix="/share/series"/>
            </intent-filter>
            <!-- Custom scheme for shared piece/series links (see src/modules/share in the
                 backend) — unlike App Links this needs no domain/fingerprint verification,
                 so "open the app if installed" works before that's set up. Not autoVerify:
                 there's no domain association to verify for a custom scheme. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="studio3"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action
                android:name="android.intent.action.PROCESS_TEXT"/>
            <data
                android:mimeType="text/plain"/>
        </intent>
        <!-- Required for url_launcher's canLaunchUrl/launchUrl to detect
             SMS and https handlers on Android 11+ package visibility. -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="sms"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>